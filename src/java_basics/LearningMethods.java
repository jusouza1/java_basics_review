package java_basics;


public class LearningMethods {

    public static void main(String[] args) {
       MyUtils myUtils = new MyUtils();
       myUtils.add10(10);
       MyUtils.printSomeJunk(10);
    }

    // void = methods dont return anything
    // first line = method signature
    // private = can only be used locally -> inside class (encapsulating);
    // access modifier = can be used at class level and on methods
    // private Class - can only be used in the same package
}
